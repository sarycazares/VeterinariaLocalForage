/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        minimumCacheTTL: 0,
        domains: [
            'img.icons8.com',
        ]
    },
}

module.exports = nextConfig

